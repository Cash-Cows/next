//types
import type { NetworkConfig } from 'modules/web3/types';
//hooks
import useAvatar from './useAvatar';
import useTabs from './useAvatar';

export default function useLeaderboard(network: NetworkConfig, address: string) {
  
};